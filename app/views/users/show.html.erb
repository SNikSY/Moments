<h1>Профиль пользователя <%= @user.username %></h1>
<p><strong>Имя:</strong> <%= @user.full_name %></p>
<p><strong>О себе:</strong> <%= @user.bio %></p>
<p><strong>Дата последнего входа:</strong> <%= @user.last_login.strftime('%d %B %Y, %H:%M') %></p>

<h2>Моменты пользователя:</h2>
<% @moments.each do |moment| %>
  <div class="moment">
    <h3><%= link_to "Просмотр момента", moment_path(moment) %></h3>
    <p><%= truncate(moment.content, length: 100) %></p>
    <p><small>Создано: <%= moment.created_at.strftime('%d %B %Y, %H:%M') %></small></p>
  </div>
  <hr>
<% end %>

<%= link_to 'Вернуться на главную', root_path %>
